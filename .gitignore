# App Store Connect API Key
.p8/

# Android signing (upload keystore + contraseñas)
.keystore/
android/keystore.properties

# Google Cloud service account JSON (Play Store API uploader)
.gcloud/

# Store screenshots (pesados, no versionados)
docs/stores/screenshots/
# Store assets generados (regenerables desde scripts/generate-*.mjs)
docs/stores/feature-graphic/
docs/stores/play-icon-512.png

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env*

# Claude Code
.claude/settings.local.json
.claude/scheduled_tasks.lock

# CDN output (generado por npm run generate-cdn)
cdn-output

# Layout check output (generado por npm run layout-check)
layout-check-output

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
